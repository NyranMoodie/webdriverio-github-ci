name: Node.js CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: switch folders and install dependencies
      run: npm install
      working-directory: tests/Schedular.UI/automation
    - name: run tests
      run: npm test   
      working-directory: tests/Schedular.UI/automation
